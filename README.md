# OpenAPITemplate.jl

A [PkgTemplates.jl](https://github.com/JuliaCI/PkgTemplates.jl) plugin that
scaffolds Julia REST/JSON API wrapper packages.

The generator produces a two-layer architecture:

- `src/api/` — generated by [OpenAPI Generator](https://openapi-generator.tech/)
  (`julia-client`), committed as plain Julia, never re-run at end-user runtime.
- `src/client/` — hand-written ergonomic overlay providing a `Client` struct,
  composable middleware, auth strategies, pagination iterators, and pretty
  printing.

## Quick start

```julia
using PkgTemplates, OpenAPITemplate

t = Template(;
    user = "your-username",
    plugins = APIWrapper(;
        spec_url = "https://petstore3.swagger.io/api/v3/openapi.json",
    ),
)
t("PetstoreClient")
```

`spec_url` may be omitted to scaffold a hand-written-only package, or pointed
at a local file path. Swagger 2.0 specs are auto-converted to OpenAPI 3.0
(the docs browser requires OAS 3+).

## What you get

A scaffolded package ships with:

### Runtime surface

- `Client` wrapping `OpenAPI.Clients.Client` with composable middleware
- `NoAuth`, `BearerToken`, `APIKey`, `BasicAuth` strategies + `resolve_credentials`
  helper that reads env vars or `~/.config/<pkg>/credentials.toml`
- `RetryPolicy` (exponential backoff + jitter, honours `Retry-After`),
  `TokenBucket` rate limiter, `with_timeout`, secret-redacting `with_logging`
- Typed `APIError` hierarchy (`NetworkError`, `ClientError`, `ServerError`,
  `AuthError`, `RateLimitError`, `TimeoutError`) with `check_response` mapper
- `default_middleware` / `with_defaults` to compose the reliability stack
- Lazy pagination iterators (`paginate_cursor`, `paginate_offset`,
  `paginate_pagenum`)
- Pretty multi-line `show` for `OpenAPI.APIModel` subtypes

### Tooling

- `gen/regenerate.jl` for re-running codegen against an updated spec
- `.github/workflows/regen-check.yml` (weekly + manual) opens a PR if the
  upstream spec drifts
- `test/` with auto-walking `runtests.jl`, opt-in Aqua/JET linting, opt-in
  BrokenRecord cassettes, opt-in Mocking helpers
- `docs/` powered by DocumenterVitepress with a per-tag interactive REST
  browser via [`vitepress-openapi`](https://github.com/enzonotario/vitepress-openapi)
- GitHub Actions for CI, docs deployment, CompatHelper, TagBot

## Maintainer prerequisites

Scaffolding **with** a spec needs Java 11+ and Node 18+ on PATH. End users
of the generated package never need either — the committed `src/api/` is
plain Julia source. The drift-check workflow is the only CI job that needs
Java; PR builds skip it.
